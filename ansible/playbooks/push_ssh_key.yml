---
- name: Push SSH key to all hosts
  hosts: all
  become: true

  tasks:
    - name: Install authorized SSH key
      ansible.builtin.authorized_key:
        user: "{{ ansible_user }}"
        key: "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_ed25519.pub') }}"
        state: present