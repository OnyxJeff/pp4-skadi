labels:
  device: skadi

steps:
  - name: uname
    image: alpine
    commands:
      - echo "Running test on Skadi"
      - uname -a
    when:
      labels:
        linux: arm64
      event:
        - push